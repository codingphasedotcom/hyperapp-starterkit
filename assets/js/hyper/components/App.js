import {h, app} from 'hyperapp'

export default function App({state, actions}) {
  return (
    <div class={'app'}>
      <h1>Hyperapp Starter Kit</h1>
      <h4>by CodingPhase.com</h4>
    </div>
  )
}
// <Header state={state} actions={actions}/>
// <Button state={state} actions={actions}/>
